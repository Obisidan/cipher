[package]
name = "cipher-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
getrandom = "0.2"

[features]
default = ["std"]
std = []
